apiVersion: 1

datasources:
  - name: Loki
    type: loki
    uid: loki             # fixed UID so the dashboard log panel can reference it
    access: proxy
    url: http://loki:3100
    jsonData:
      maxLines: 1000
